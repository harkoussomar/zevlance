// ─── Components ─────────────────────────────────────────────────────────────
// These are the only two placed directly in pages by the app shell
export { ProjectListPanel } from "./components/ProjectListPanel";
export { ProjectDetailPanel } from "./components/ProjectDetailPanel";
export { ProjectCard } from "./components/ProjectCard"; // used on homepage grid
